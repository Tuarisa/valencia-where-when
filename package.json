{
  "name": "valencia-where-when",
  "version": "1.0.0",
  "private": true,
  "description": "Valencia Radar — afisha, places & notifications for Valencia (RU + city events). Node.js rewrite of the Python prototype.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "db:schema": "node scripts/apply-schema.mjs",
    "db:seed": "node scripts/seed.mjs",
    "db:setup": "node scripts/apply-schema.mjs && node scripts/seed.mjs",
    "pipeline:run": "node scripts/run-pipeline.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "next": "14.2.15",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
