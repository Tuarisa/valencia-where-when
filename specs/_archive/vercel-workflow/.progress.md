---
spec: vercel-workflow
basePath: specs/vercel-workflow
phase: research
task: 0/0
updated: 2026-06-20T13:02:43Z
---

# Progress: vercel-workflow

## Original Goal

vercel-workflow — Vercel Workflow SDK используй для задач, потому что все будет крутиться на сервере потом

## Completed Tasks

_No tasks completed yet_

## Current Task

Research complete — research.md written. Awaiting approval to proceed to requirements.

## Learnings

- Vercel Workflow SDK is REAL + GA. Package `workflow` (`npm i workflow`); directives `'use workflow'` / `'use step'`. Builds on open-source workflow-sdk.dev. Runs on Vercel Functions + Vercel Queues. Verified via Vercel MCP docs + workflow-sdk.dev.
- Trigger: `import { start } from "workflow/api"` → `await start(fn, [args])` → `{ runId }` (fire-and-forget, returns fast). NO built-in cron/schedule primitive — still need Vercel cron or GH Actions to call start().
- WORKS ON HOBBY: 50K events/mo + 1GB written/mo included; only Data Retained is Pro-only. Hobby run-log retention = 1 day. Max run duration = NO LIMIT, max sleep = NO LIMIT → escapes the 300s maxDuration cap.
- Hobby 1-cron/day limit is UNCHANGED by Workflow (the "Schedules/cron: No limit" in pricing is a platform-resource note, not the project-cron-plan limit). Spec 001 D1 GH-Actions scheduler still required.
- Existing stage fns reusable as steps with a 1-line `'use step'` directive — no body rewrite. Workflow fn must be deterministic (IO only inside steps). Fan-out = `Promise.all` of step calls; per-step retry-on-throw = native fail-soft for enrichment (constitution IV).
- Next.js setup: wrap config with `withWorkflow(nextConfig)` from `workflow/next`. Local dev runs under `next dev` (steps/sleeps work locally); observability via `npx workflow web` / `npx workflow inspect runs`. NOTE: the offline `.mjs` `pipeline:run` script is OUTSIDE Workflow → keep it for local-run.
- GOTCHA: constitution says "stack is fixed" → adding Workflow SDK needs a constitution amendment (MINOR bump) before adoption.

## Blockers

- None currently

## Next

Complete research, then proceed to requirements
