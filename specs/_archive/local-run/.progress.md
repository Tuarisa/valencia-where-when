---
spec: local-run
basePath: specs/local-run
phase: research
task: 0/0
updated: 2026-06-20T12:46:15Z
---

# Progress: local-run

## Original Goal

local-run — мне нужно чтобы ты мне сделал возможность полноценного локального запуска перед деплоем на vercel

## Completed Tasks

_No tasks completed yet_

## Current Task

Starting research phase

## Learnings

- DB driver is `@neondatabase/serverless` 0.10.4 (HTTP/WS), NOT TCP `pg`. App+pipeline use ONLY tagged-template `sql`...``; 2 scripts use `sql.query(text,values)`. Both supported by `neon()`.
- DECISION: local Neon HTTP proxy (`TimoWilhelm/local-neon-http-proxy`) + Dockerized Postgres + a host-gated `neonConfig.fetchEndpoint` shim. Zero prod-path change (constitution VI). Fallback: real Neon dev branch in `.env.local`.
- Neon Local (`neondatabase/neon_local`) is NOT offline — needs NEON_API_KEY+PROJECT_ID, proxies to cloud branches. Rejected.
- Driver abstraction (`pg` local) rejected: `pg` has no tagged-template API → rewrite all 8 `sql`...`` files; drift risk.
- Docker is NOT installed on this machine (hard prereq for the chosen path). `psql`/`pg_ctl` present but Neon driver can't use them without the proxy. `claude` CLI v2.1.177 present. Node v24.14.0.
- `scripts/run-pipeline.mjs` is currently BROKEN locally: needs `tsx` which is NOT installed (absent in node_modules + package.json). Add `tsx` devDep.
- Offline-capable stages: normalize, dedup, score, tag (pure DB+CPU). Network-required: ingest (t.me/web/CACSA) + geo (Nominatim). Cron route runs the FULL pipeline → needs internet.
- GOTCHA: weekly digest/alert endpoints and a `claude -p` enrichment script DO NOT EXIST yet — only `/api/cron/refresh`. Goal assumes them runnable; they must be built or scoped out. Surface in requirements.
- Scripts read `DATABASE_URL` from env only (no dotenv) → must `set -a && source .env.local`. Wrap in a `dev:local` script/Makefile.
- Seed expected counts: sources 22, events 342, places 14, media_assets 216.

## Blockers

- None currently

## Next

Complete research, then proceed to requirements

## User Answers (2026-06-20T12:59:50Z)
- Docker: INSTALL (Docker Desktop installing via brew now) → Tier A docker-compose is the path.
- Digest/alert/enrich endpoints: OUT OF SCOPE → own specs later. local-run = DB + pipeline + /api/cron/refresh + site.
- Offline: not required, internet OK. Run in Docker "as if Vercel" to debug before deploy (Vercel-parity container).
- claude -p auth: acceptable, even on Vercel.
