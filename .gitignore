# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
# exception: bundled PUBLIC intermediate CA needed at runtime (T148, cac.es chain completion — not a secret)
!certs/*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# claude code
.claude/

build.log
specs/.current-spec
**/.progress.md

# crawler fetch cache (raw HTML, re-fetched on demand) — keep out of git
data/.cache/

# export-seed.mjs scratch output (T144) — not a deliverable, regenerate from the live DB
data/seed-export/
# rebake-seed.mjs scratch output (T173) — not a deliverable, regenerate from the live DB
data/seed-rebake/
